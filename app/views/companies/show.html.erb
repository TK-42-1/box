	<%= link_to @company.id, @company %> <%= @company.name %>
		| <%= link_to "edit", edit_company_path(@company) %>
	<% if current_user.admin? %>
	| <%= link_to "delete", @company, method: :delete, data: { confirm: "Confirm deletion"}%>
	<% end %>
<ul>
	<% @company.departments.each do |dept| %>
	<li><%= dept.name %></li>
	<% end %>
</ul>